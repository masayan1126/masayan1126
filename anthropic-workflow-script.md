# Anthropic公式ツールだけで作るChrome拡張機能開発ワークフロー 読み上げ台本

> 想定尺: 約19〜21分
> 対象視聴者: Claude Code ユーザー、Chrome拡張機能開発者
> スライド: 全15枚

---

## 冒頭A（0:00〜1:00）— 導入：何を見せるのか

【スライド1: タイトル「Anthropic公式ツールだけで構築する開発ワークフロー」/ 題材: Rich Markdown Preview Issue #10】

以前別の動画で、Anthropicの公式スキルやプラグインの概要を紹介しました。
要件定義を対話で作るスキル、コードレビューを並列で走らせるスキル、
セキュリティスキャンを自動で実行するプラグイン——
こういったものが公式に提供されています、という話をしました。

ただ、概要だけだと「で、実際どう使うの？」がわからないんですよね。
なので今回は、実際の開発の中で使っている流れをそのまま見せることで、
もう少し解像度を上げられればと思います。

【スライド2: 本日の流れ（01 導入〜07 ドキュメント+まとめ の7項目目次）】

今日は、それらの公式ツール「だけ」を使って、
GitHubで管理しているソースコードの機能改修を、
要件定義から設計・実装・レビュー・ドキュメントまで
全工程を通しでやってみます。

【スライド3: 本日やること（01 公式ツールのみ / 02 サードパーティ不使用 / 03 全7フェーズを完走）】

サードパーティのツールはほとんど使いません。
【強調】公式ツールだけで、開発ライフサイクル全体がどこまで回るのか。
それを実際に見てもらいます。

---

## 冒頭B（1:00〜2:00）— 問題提起：今回のお題

【スライド4: Issue #10 同名ファイルが別タブで開けない（根本原因の構造 + 修正前の動作図）】

題材は、自分が開発しているChrome拡張「Rich Markdown Preview」です。

スライドに書いてある通り、
dir1のREADME.mdを開いた後、dir2のREADME.mdを開こうとしても、
タブが増えず、既存のタブがアクティブになるだけです。
【強調】異なるディレクトリにある同名ファイルが、同一ファイルとして扱われてしまう。
これがIssue #10です。

このバグ修正を、全7フェーズ、すべてAnthropicの公式ツールで完結させます。

---

## 開発ライフサイクルの全体像（2:00〜3:30）

【スライド5: 開発ライフサイクル全体像（①要件定義〜⑦ドキュメント + ガードレール のツール対応表）】

使うツールは、このテーブルに全部書いてあります。
これを CLAUDE.md というファイルに書いておくと、
Claude Code がセッション開始時に読み込んで、
各フェーズで自律的にどのツールを使うか判断してくれます。

簡単に整理すると——
①要件定義は doc-coauthoring。
仕様が確定したら①' として gh issue edit で Issue を上書きします。
②設計は feature-dev。デザインが必要なら frontend-design も使えますが、今回は使っていません。
④実装も feature-dev。
⑤コミットとPR作成は commit-commands:commit-push-pr。
⑥レビューは pr-review-toolkit:review-pr。
そして⑦ドキュメントは claude-md-management:revise-claude-md と internal-comms。

【強調】全部 Anthropic 公式です。サードパーティのツールはほとんど使いません。

---

## ガードレール（3:30〜5:00）

【スライド6: ガードレール（hookify / security-guidance / claude-md-management の3つの安全策）】

フェーズに入る前に、ガードレールを設定します。
2種類あります。

1つ目は hookify です。今回は使っていませんが、紹介だけしておくと——
「テスト実行前にビルドを確認せずにコミットしないこと」
のように自然言語でルールを伝えると、
設定ファイルが自動生成されて、次のセッションから適用されます。

2つ目は security-guidance というプラグインです。
settings.json で有効化しておくと——
ファイルを編集するたびに、【強調】自動でセキュリティスキャンが走ります。
XSSやインジェクション系のパターンを検知すると——
ファイルの書き込み自体をブロックしてくれます。
コードを書いた瞬間にリアルタイムで止まるんですよね。

【強調】実はこの動画のブログ記事を書いているときに、2回フックが発火しました。
1回目は、セキュリティ警告の説明文の中に「特定のキーワード」を書いたとき。
2回目は、ReactのXSS対策に関する解説文を書いたとき。

コードじゃなくて【強調】ドキュメントを書いているだけで止まる。
それくらい厳密にチェックが走っています。

これが全フェーズを通じて常時動いています。

---

## フェーズ①：要件定義（5:00〜6:30）

【スライド7: フェーズ① 要件定義 /doc-coauthoring（対話形式の仕様書作成フロー + 成果物5セクション）】

では、本題に入ります。まず要件定義から。
スラッシュ doc-coauthoring を呼び出します。
Claude Code が対話形式で仕様書を一緒に作ってくれます。

たとえばこんなやり取りになります。
Claude から「解決したい問題を具体的に教えてください」と聞かれて——
自分は「異なるフォルダにある同名ファイルを同時にタブで開けない」と答えます。

すると次に「影響するコンポーネントはどのあたりですか? タブ管理・サイドバー・D&Dのどれですか?」
こういう質問が来るんです。

こうやって一問一答で、背景・ユーザーストーリー・受け入れ条件・影響範囲・制限事項——
5つのセクションを詰めていく。

【強調】自分で構成を考えなくていい。質問に答えるだけで仕様書が完成します。
今回の場合、成果物は docs/issue-10-same-name-file-spec.md として保存されました。

そして仕様が確定したら——
GitHub CLIでIssueのタイトルと説明を、確定した要件で上書きします。

【スライド8: Issue #10 確定した要件でIssueを上書きした画面（GitHub Issue実物のスクリーンショット）】

【強調】IssueとコードベースのドキュメントをAIが橋渡ししてくれる形です。

---

## フェーズ②：設計（6:30〜8:00）

【スライド9: フェーズ② 設計 /feature-dev（案A: ユニークID vs 案B: isSameEntry() ★採用）】

次は設計です。feature-dev を呼び出します。

ここでもエージェントが【強調】並列で動きます。
code-explorerがコードベースを調査して根本原因を特定し、
code-architectが設計案を組み立てる——この2つが同時に走ります。
レビューフェーズの3エージェント並列と同じ構造です。

code-explorerがパス構築と重複チェックの2箇所に根本原因があることを突き止めて、
code-architectがスライドにある通りA/Bの2案を提示してきました。

自分は【強調】Bを選びました。
型に余計なフィールドを増やさない方が、あとで不整合が起きにくいからです。

この選択は、後のレビューフェーズで正解だったことが確認されます。
設計の成果物はプランファイルとして自動保存されます。

---

---

## フェーズ④：実装（9:30〜14:00）

【スライド10: フェーズ④ 実装（5ファイル修正一覧 + ビルド・テスト・リリース準備手順）】

実装フェーズです。修正するのは【強調】5つのファイルです。
スライドに一覧がありますが、ざっくり言うと——

パス構築の根本修正、ファイル同一性判定の改善、タブラベルの表示変更、
競合状態の防止、エラーログの追加。
この5点を feature-dev が一括でコード生成してくれました。

ビルドとテストも実行して、【強調】Vitestの73件が全パス。
バージョンを上げてzip再作成して、実装フェーズ完了です。

---

## フェーズ⑤：コミット/PR（14:00〜15:30）

【スライド11: フェーズ⑤ コミット/PR（自動生成されたコミットメッセージ全文 + PR #13）】

実装が終わったら、スラッシュ commit-commands:commit-push-pr 一発です。
【強調】コミット・プッシュ・PR作成が一度に終わります。

生成されたコミットメッセージはこうなりました。
「fix: 同名ファイルを別タブで開けない問題を修正 (#10)」

箇条書きはこの5つです。

【強調】1つのコマンドで、変更の全体像が整理されたコミットメッセージが出来上がる。

そして PR #13 が masayan1126/chrome-extensions に自動でオープンされました。
概要・変更ファイル一覧・テスト手順が入った本文が生成されています。

---

## フェーズ⑥：レビュー（15:30〜18:00）

【スライド12: フェーズ⑥ レビュー（3エージェント並列実行 → 3件の指摘 → 「1」で全修正）】

次はレビューです。pr-review-toolkit を呼び出すと——
この3つが【強調】同時に走ります。
code-reviewerはコード全体の品質、
type-design-analyzerは型設計の妥当性、
silent-failure-hunterはエラーの握りつぶしを、それぞれ専門の観点でチェックします。
人間がレビューするなら順番に見ていくところを、並行でやってくれる。

【強調】それぞれ違う観点でチェックしているので、精度が高い。
今回は3つの問題が見つかりました。
型の冗長なフィールド、エラーの握りつぶし、例外処理の粒度不足。
どれも自分では気づいていなかった指摘です。

人間がやるのは、この指摘を確認して、どう対応するか判断するだけ。
自分がタイプしたのは【強調】「1」の1文字だけです。
「全部直して」という意味で1を選ぶと、全修正が一括で走ります。

---

## フェーズ⑦：ドキュメント（18:00〜19:30）

【スライド13: フェーズ⑦ ドキュメント（CLAUDE.md 3回更新 + リリースノート2種類自動生成）】

最後はドキュメントです。2つのスキルを使います。

まず claude-md-management:revise-claude-md 。今回のセッションで学んだことを CLAUDE.md に反映します。

実はこの CLAUDE.md 、セッション中に【強調】3回更新されました。

1回目は、doc-coauthoring で要件を確定した後に
Issue を更新するフローを追加したとき。

2回目は、リリースノート作成用の internal-comms を
ライフサイクルに加えたとき。

3回目は、「実装変更後は必ずzipを作り直すこと」という
独立した箇条書きを——ライフサイクルの文脈に自然に溶け込むよう書き直したとき。

こうやって会話の中で自然に育っていく。
【強調】次回以降のセッションで全部引き継がれます。

次に internal-comms でリリースノートを作ります。
GitHub Releases向けの技術者向けバージョンと、
Chrome拡張ストア向けの一般ユーザー向けバージョン、
【強調】2種類が一度に生成されます。

技術者向けはこんな文体です。
「FileSystemFileHandle.isSameEntry() API を使った正確なファイル同一性判定に変更。
directoryReader にてフルパスを正しく構築するよう修正——」

一方、Chromeストア向けはこうなります。
「フォルダが違えば同じ名前のファイルでも、それぞれ別タブとして開けるようになりました。」

【強調】同じ内容を、読む人に合わせて自動で書き分けてくれる。
これが internal-comms の価値です。

---

## エンディング（19:30〜20:30）

【スライド14: バグ修正確認とまとめ（修正前→修正後の対比 + 7フェーズ完走チェックリスト）】

最初のバグを見てみましょう。
今度は両方が別タブで開けています。
タブに dir1/README.md と dir2/README.md が表示されていて、
どちらがどのファイルか一目で分かります。

今日やったことを振り返ると——
要件定義・設計・デザイン・実装・コミット/PR・レビュー・ドキュメント、
【強調】7フェーズをすべて Anthropic 公式ツールだけで完結させました。

【スライド15: 2回の判断で7フェーズが完走した（H: オーケストレーション・ハーネス整備・Why/What構築 / AI: 自動生成・並列レビュー・一括修正 / +: 指示/判断の質が開発の質を決める）】

今日のセッションで、人間がタイプした判断は「Bで」と「1」——たった2回です。
それだけで、仕様書・コード・PR・リリースノートが自動生成され、
役割別のエージェントが並列でレビューを実行し、5ファイルの修正とテスト全パスまで完了しました。

じゃあ人間は何をしていたのか。

エージェントをどう組み合わせるかというオーケストレーション。
CLAUDE.mdやフックといったハーネスの整備と、公式ツールのキャッチアップ。
そしてAIに質の高いコンテキストを渡すための、WhyとWhatの構築。

【強調】書くコードの量ではなく、下す判断の質が、開発の質を決める時代になっていく——
そう感じてもらえたら嬉しいです。

ぜひ参考にしてみてください。

見ていただきありがとうございました。

---
